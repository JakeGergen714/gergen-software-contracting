services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: client_portal
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  keycloak:
    image: quay.io/keycloak/keycloak:25.0
    command: ["start-dev", "--import-realm"]
    environment:
      KC_DB: dev-file
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    volumes:
      - ./keycloak/realm-export:/opt/keycloak/data/import
      - keycloakdata:/opt/keycloak/data
    ports:
      - "8081:8080"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/realms/client-portal/.well-known/openid-configuration"]
      interval: 10s
      timeout: 5s
      retries: 60

  api:
    build:
      context: ./backend
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/client_portal
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
      KEYCLOAK_ISSUER_URI: http://keycloak:8080/realms/client-portal
      CORS_ALLOWED_ORIGINS: http://localhost:5173
      DISABLE_AUTH: "false"
      SPRING_PROFILES_ACTIVE: "test"
    depends_on:
      - postgres
      - keycloak
    ports:
      - "8080:8080"
    develop:
      watch:
        - action: rebuild
          path: ./backend/pom.xml
        - action: rebuild
          path: ./backend/src

  portal:
    build:
      context: ./portal
      dockerfile: Dockerfile
      target: dev
    environment:
      # Ensure reliable file watching/HMR inside Docker
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
      VITE_KEYCLOAK_URL: http://localhost:8081
      VITE_KEYCLOAK_REALM: client-portal
      VITE_KEYCLOAK_CLIENT_ID: client-portal-web
      VITE_API_BASE_URL: http://localhost:8080
      VITE_PORTAL_BUSINESS_ID: 11111111-1111-1111-1111-111111111111
    ports:
      - "5173:5173"
    depends_on:
      - api
    command: ["npm","run","dev","--","--host","0.0.0.0"]
    develop:
      watch:
        - action: sync
          path: ./portal
          target: /app
          ignore:
            - node_modules/
            - dist/
            - .git/
            - .vite/
        - action: rebuild
          path: ./portal/package.json
        - action: rebuild
          path: ./portal/package-lock.json

  website:
    build:
      context: ./website
      dockerfile: Dockerfile
      target: dev
    environment:
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
    ports:
      - "5174:5173"
    command: ["npm","run","dev","--","--host","0.0.0.0"]
    develop:
      watch:
        - action: sync
          path: ./website
          target: /app
          ignore:
            - node_modules/
            - dist/
            - .git/
            - .vite/
        - action: rebuild
          path: ./website/package.json
        - action: rebuild
          path: ./website/package-lock.json

volumes:
  pgdata:
  keycloakdata:
